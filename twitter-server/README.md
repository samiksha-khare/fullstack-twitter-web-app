1. yarn init --> press enter all all fields --> package.json is created.
2. Make src folder --> add dependencies --> yarn add typescript -D and create index.ts file.
3. Initalize typescript config --> tsc --init --> uncomment "rootDir": "./src", "outDir": "./build"
4. Add dependecies: yarn add express
5. Add custom scripts: start, build, dev

